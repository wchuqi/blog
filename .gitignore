node_modules
dist
dist-ssr
*.local
.DS_Store
*.log
tsconfig.tsbuildinfo
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright CLI artifacts
.playwright-cli/
output/

# 本地复习数据库（不入仓库，只在本机）
review.db
review.db-journal
__pycache__/
*.pyc

# 本地工具 / AI 会话目录（不入仓库）
.claude/
.opencode/
.design-audit/
plans/
src/posts/.solomd/

# 下载资源（skill: video-to-markdown 的产物，不入仓库）
.download/

# 凭据（skill: video-to-markdown 用，绝不入仓库）
.env
.cookie

# dancer 的 vendor 会往 cwd 写 .cookies.json（抖音 Cookie），
# 手动从仓库根跑 jjdown 时会落到这里
.cookies.json
